树的概念
    节点，根节点，父节点，子节点，兄弟节点
    一棵树可以没有任何节点，成为空树
    一棵树可以只有一个节点，也就是根节点。
    子树，左子树，右子树 ：左边的树，右边的树
    节点的度：degree  子树的个数，针对这个节点，他的子树。就是子节点的个数。
    树的度，所有节点度中的最大值
    叶子节点：度为0的节点
    非叶子节点：度不为0的节点


二叉树：binary tree
    每个节点的度最大为2
        他的度，也就是子节点，子树只有3中可能，0，1，2
    左子树和右子树有顺序
        左子树小于右子树，即使只有一个子树，也要严格区分左右子树。要严格区分，字数顺序不同，也是另外一棵树。
    即使只有一个节点，也要区分左右子树。
    二叉树有序树。

    二叉树的性质：
        非空二叉树第i层，最多有2^（i-1）个节点
        高度为h的二叉树最多有2^h - 1 个节点，如果层数或者高度为i，那么第i层最多有2^i-1各节点。
        叶子节点（度为0）的接待那个数为n1，度为2的节点个数为n2 那么n1 = n2+1

    真二叉树
        所有节点要么度为2，要么度为0

    满二叉树
        所有节点的度，要么为0，要么为2，在二叉树的基础上，所有叶子节点都在最下层。每一层都填满了。
        满二叉树就是没有空了。加一个节点，必须增加一层。

    完全二叉树：
        叶子节点只会出现最后2层，且最后一层的叶子节点靠左对齐。
        满二叉树是完全二叉树。完全二叉树只能从左往右排。倒数第二层是满的，最后一层从左到右，
        性质：
            度为1的节点只有左子树
            度为1的节点要么是1个，要么是0个
            同样节点的二叉树，完全二叉树高度最低。
            如果知道高度，h
            那么至少有2^h-1个节点（因为倒数一层为满二叉树，节点数量为2^h-1 -1）
            最多有2^h -1个节点
            知道节点个数，可以推算高度。h = floor(log2n) + 1


