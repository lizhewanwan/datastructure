大O表示法
一般用大O表示法，对应规模n对应的复杂度。
忽略低阶，常数，系数。

i ++    on
i /2    logn
i += i  logn
i *= 2  logn

注：i +=1 i = i+ 1  都是一次
   i += i i = i*2   logn

1 <  logn < n < nlogn < n2 < n3 < 2^n



优化方向：佣金可能少的存储空间，尽可能少的计算次数
具体的业务要求中，牺牲时间换空间，牺牲空间换时间。

多个数据规模的情况：
for i in range(n):
    pass
for j in range(k):
    pass
O(n+k)


最好最坏复杂度
均摊复杂度
复杂度震荡
平均复杂度