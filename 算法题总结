1，遇到数组的题，
    1，动态数组就是python中list，java中的ArrayList。可以直接用索引。然后想办法用字典把索引和值保存起来。
2，遇到链表的题。
    1，就是一个个node。逻辑上相连。每次取值，都要先用next找一遍，找到目标值。
    2，命名最好命名成pre，cur，next然后互相替换。
    3，快慢指针操作。
    4，加个头结点，让代码简练。newHead = Node(None) # 可以让头节点和其他节点一样的处理逻辑。