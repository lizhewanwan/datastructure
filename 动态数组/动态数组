1，线性表
    具有n个相同元素的有限序列。
   首节点，尾节点，前驱，后继。
   常见线性表有：
        数组，链表，栈，队列，哈希表


数组：
    是一种顺序存储的线性表，所有元素的内存都是物理连续的。
    有利于查询，不利于修改。
    无法动态更改容量。存的东西都是同类型。

动态数组：
    修正数组不能动态扩容和缩容的缺点。
    动态扩容，乘1.5 也就是 capcity + capcity >> 1
    在python中，list是动态数组，存的是对象的指针(地址)。所以可以存各种类型的数据。


python 中list  就等价于 java ArrayList<object>

list_python = list()
ArrayList<object> list_java = new ArrayList<object>();


# 遇到数组有关的题，首先想到列表。然后用字典把指针和值储存起来。

    最好  最坏  平均（所有可能的复杂度相加/所有可能的个数表达式，去除系数，常数，低阶）
增：o(1)  o(n)    o(n)    # 找到位置，然后所有的往后移，空出来增加
删：o(1)  o(n)    o(n)    # 找到位置，所有后边的往前移
查：o(1)  o(1)    o(1)   # 告知索引，index，地址=头结点地址+4*index o(1)复杂度
改：o(1)  o(1)    o(1)


